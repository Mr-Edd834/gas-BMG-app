// Shared layout constants — see spec Part C §1 "Home/Sales" §1 (Shared components, Interaction rules).
export const touchTarget = 44; // minimum touch target size, px
export const stepperCircle = 32; // stepper +/- circle diameter, px
export const cardRadius = 14; // card corner radius, mid of the 12-16px range
export const cardPadding = 16; // card padding, mid of the 12-20px range
export const cardBorderWidth = 1;
