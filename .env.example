# Copy to .env and fill in from the Supabase project dashboard (Settings > API).
# Only the anon/public key ever belongs here — never the service_role key
# (spec Part A §5.2). Expo exposes vars prefixed EXPO_PUBLIC_ to the client bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
